// Print all integers from 1 to 255

/*  Step1 create an empty array 
    Step2 generate values from 0 to 255 
    Step3 add each value to our empty array 
*/

let arr = [];
for (val = 0; val <= 255; val++)
{
    arr.push(val)
}
console.log(arr)